data = ["alpha", "beta", "gamma"]

idata = iter(data)

print(f"next(idata): {next(idata)}")
print(f"next(idata): {next(idata)}")
print(f"next(idata): {next(idata)}")
print(f"next(idata): {next(idata)}")
